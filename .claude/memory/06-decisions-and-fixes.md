---
name: decisions-and-fixes
description: سجل القرارات المتخذة والأعطال التي حُلّت وسببها الحقيقي، والمهام المؤجَّلة. اقرأه قبل «إصلاح» أي شيء — قد يكون مُصلحًا بالفعل أو مقصودًا.
---

# سجل القرارات والأعطال

## أعطال حُلّت — لا تُعِد فتحها

### مشروع بلوجر يظهر «مكتمل» من أول مرحلة
قاعدة البيانات تضع لأي مشروع جديد `timeline_step = 'contract_payment'`، بينما
كانت قائمة مراحل بلوجر تسمّي أول مرحلة `blog_contract_payment`. الاسم المختلف
جعل `indexOf` يرجع `-1`، فظهر الزر «اكتمل المشروع» وتعطّل «إرسال التقدم».
الحل: توحيد الاسم إلى `contract_payment` (نفس ما تفعله قائمة الصيدلية عمدًا).

### اختفاء بطاقة معاينة رابط الدخول على واتساب
`Disallow: /auth` أُضيف ضمن شغل السيو في Commit `8dbe76f`، وروبوت المعاينة
يحترم `robots.txt`. الحل: `Allow: /auth/enter` قبله. التفاصيل في `05-…`.

### خريطة الموقع في Google Search Console
كان الخطأ «تعذّرت قراءة خريطة الموقع» مع 0 صفحات. تم التحقق في 2026-09-02:
الحالة الآن **«تم الإجراء بنجاح» و10 صفحات مكتشفة**. لا تُعِد بناء الخريطة.

### ارتداد: تبويب «النخبة» في شاشة الدخول
حُذف عمدًا في `ea05c99`، ثم **أعادته عن غير قصد** جلسةٌ لاحقة في Commit
`bd94902` بعنوان «فحص شامل للمنصة». حُذف نهائيًا في `4b4c40b`.

القاعدة المستخلصة: الـ Commits الكاسحة من نوع «فحص شامل» تُعيد كودًا محذوفًا
عمدًا. قبل أي مراجعة واسعة افحص `git log -S "<النص>"` وتأكد أن ما تضيفه لم يُزله
المالك من قبل.

## قرارات ثابتة

- **واتساب فقط** للعميل — راجع `05-whatsapp-only-channel.md`.
- **باقتا بلوجر** بأسعار وخطط دفعات محددة — راجع `04-portal-contract-packages.md`.
- بنود العقد 6 و7 و8 ووصف الدفعة الثالثة ومرحلتا المحتوى **تتفرّع حسب الباقة**؛
  الأساسية تحتفظ بنصها الأصلي حرفيًا.
- الكرت المميَّز يعرض شارة واحدة فقط («الأكثر طلبًا») — الشارتان معًا تتراكبان.
- ألوان كرتَي بلوجر مربوطة بالحالة `package-grid-two` حصرًا، فلا تتأثر باقات
  المنصات (4 كروت) والصيدلية (3 كروت).
- الفوتر: رقما مصر والسعودية **واتساب فقط**، في سطرين متساويي العرض، بلا أيقونة
  تليفون (كانت تناقض النص).

- **kareempro.com للبرمجة والتطوير فقط (قرار كريم 2026-09-24).** يُعاد بناء الصفحة
  الرئيسية `public/index.html` لتكون برمجة فقط (منصات · متاجر · تطبيقات جوال ·
  مواقع · مدونات)، ويُشال منها المونتاج والتعليق الصوتي والإبداع البصري **من
  الجذور** — نصوص وأعمال وملفات وسيو، و**بدون أي رابط لـ ebda3-media.com**
  (تعديل كريم 2026-09-24: تخصص واحد للسيو). **السيو أولوية:** كل الكلمات المفتاحية
  للبرمجة ولغات البرمجة والمنصات والتطبيقات والمواقع. **المدونات جزء من الخدمات**
  (صفحة `/blog` وباقتا بلوجر موجودتان). آراء العملاء الوهمية الـ6 تُشال وتُستبدل
  بآراء حقيقية بعد موافقة أصحابها.

- **خطوة «تعرّف علينا» في عرض LINK (2026-09-25):** نص برمجة فقط (8 نقاط اختارها
  كريم بالحرف) في `SERVICE_ABOUT_OVERRIDES.link`، ومعروضة بتصميم `WhyKareemPro`
  — **الاختيار B «تحريري مرقّم»** اللي اختاره كريم من 3 نماذج (عمود جانبي بالعنوان
  + قائمة 01–08 بعمودين، عمود واحد على الموبايل). CSS مقصور على `.why-kp` في
  `portal-dashboard.css`. باقي الخدمات على العرض القديم كما هو.
- **زرّا «السابق/التالي» في العرض (كل الخدمات):** جنب بعض عند جهة «السابق»،
  أكبر (46px) بأسهم SVG؛ على الموبايل يملآن العرض مناصفة. `.funnel-nav-btn`.
- **شرط كريم:** أي تعديل في العرض يتختبر على الموبايل قبل التسليم — العملاء بيفتحوا
  الرابط من الواتساب على الموبايل.

- **صفحة الفريق في العرض (2026-09-25):** كريم رفض 3 تصاميم بديلة وطلب **الإبقاء على
  التصميم الأصلي (TeamOrbit) + لمسة إبداعية**. اتضاف: حلقتا مدار دوّارتان بمقاس
  نصف القطر الحقيقي، وخطوط متقطعة متحركة من المؤسس لكل عضو، ونبض حول صورة المؤسس
  (كلها ديسكتوب فقط، `.team-orbit-fx`). **إصلاح موبايل كان مكسور:** اسم المؤسس كان
  بيظهر تحت آخر عضو، وكروت الأعضاء كانت بإطار بيضاوي ممطوط — دلوقتي الاسم تحت
  الصورة مباشرة، والأعضاء كروت بيضا في عمودين. الـ CSS في آخر `portal-dashboard.css`.
- **مسميات الفريق في عروض البرمجة (اعتمدها كريم 2026-09-25):** أحمد شاهين = Web
  Developer · أسماء المقدم = Growth Marketer · مريم أحمد = UI/UX Designer · ندى رحيم =
  Full Stack Developer. `PROGRAMMING_ROLES` + `PROGRAMMING_SERVICES` (pharmacy · link ·
  platform · platform-apps) في `OnboardingFunnel.js`. باقي العروض (فيديو، تعليق صوتي،
  كورس، بلوجر…) على المسميات الأصلية في `TEAM_MEMBERS`.

- **«نماذج أعمالنا» في عروض البرمجة (2026-09-25):** كريم اختار التصميم 3 «نوافذ متصفح
  حيّة» من 5 نماذج. مكوّن `LiveWorks` + قائمة ثابتة `PROGRAMMING_WORKS` (نفس 6 مشاريع
  معرض الصفحة الرئيسية ونفس الصور `public/*-preview.webp` — **حدّث الاتنين مع بعض**)
  بدل كاروسيل قاعدة البيانات، لخدمات `PROGRAMMING_SERVICES` فقط. باقي الخدمات على
  الكاروسيل القديم. CSS `.live-works` في آخر `portal-dashboard.css`.
- **مفتوح:** صورة `kareempro-preview.webp` لسه عليها «روائعنا الإبداعية» (مونتاج) —
  محتاجة سكرين شوت جديد للموقع بشكله البرمجي. مستني موافقة كريم.
- **Coffee Land اتشال نهائيًا (2026-09-26):** رابطه بقى يفتح متجر تاني. اتحط مكانه مدونة iKareem
  (ikareempro.com، صورة `ikareem-blog-shot.webp`) في المعرض الرئيسي و`PROGRAMMING_WORKS`، واتشال من
  showcase في store.html و services_data.py (يدويًا). الصورة القديمة في `_to_delete/`.
- **مفتوح:** iHealth في store.html (ihealth-mu.vercel.app) بيظهر «انتهت فترة المعاينة» — محتاج قرار كريم.

## مؤجَّل / مفتوح

- **وسوم `<title>` و OG و Twitter في `public/index.html` — أُغلق (2026-09-16).**

  كان العنوان المخفي «تصميم مواقع ومتاجر إلكترونية وتطبيقات — Kareem Pro»
  مخالفًا للعنوان الظاهر «برمجة منصات ومواقع وتطبيقات الجوال». اختار كريم
  الحل الوسط: **إضافة** لا استبدال، حفاظًا على الكلمات المفتاحية المرتّبة.

  النص المعتمد في المواضع الثلاثة:
  `تصميم وبرمجة مواقع ومنصات ومتاجر إلكترونية وتطبيقات — Kareem Pro`

  القاعدة: كلمة **«تصميم»** لا تُحذف من الوسوم مهما تغيّر العنوان الظاهر —
  «تصميم مواقع» أعلى حجم بحث في السوق العربي من «برمجة منصات».
  **التذكير الأسبوعي بهذا البند أُلغي.**
- **سطر «طريقة السداد» داخل كرت الباقة الأساسية** مختصر مقارنة بتفصيل العقد.
  عُرض على كريم فرفض تفصيله.
- `resendInvite` في `src/app/admin/actions.js` لم يعد مستخدَمًا بعد إزالة زر
  «إعادة إرسال إيميل الدخول». تُرك بلا حذف.
- `src/proxy.js` ما زال يضيف `?role=client` عند التحويل من `/auth/set-password`
  — بارامتر بلا أثر بعد حذف تبويب العميل. غير ضار.
- **قرار: لا ملفات عرض خارج المنصة.** جُرِّبت معاينة للعرض الفني والمالي لمدونة
  بلوجر كملف HTML وPDF بمقاس الجوال في `design-preview/`، وحُذفت بطلب كريم
  (2026-09-14). السبب: رابط البوابة وحده هو ما يُرسَل للعميل — أي ملف منفصل
  نسخة ثانية تتقادم مع أول تغيير سعر. **لا تُنشئ بديلًا عنها.**
- **`_to_delete/claude_partial_1788394123/`** — بقايا جلسة انقطعت على الماك بوك
  (2026-09-02). غير متتبَّعة وغير مؤثرة. اسأل كريم قبل إزالتها.

### بانر المشاركة: png كان لسه في 11 مكان
`og-banner-v2.png` (647 KB) فوق حد واتساب (~600 KB) فالبطاقة تظهر بلا بانر. الإصلاح
في `b4f5985` طال `auth/enter/route.js` فقط. في 2026-09-24 اتصلّح الباقي: 9 صفحات
ستاتيك + `scripts/build_services.py` (4 مراجع) + `src/app/layout.js` (مرجعان — ده
بانر كل صفحات Next.js: البوابة والأدمن والدخول). المستعمل الآن `og-banner-v2.jpg`
(120 KB) في كل مكان، والنسخ القديمة اتنقلت لـ `_to_delete/unused-assets-2026-09-24/`.

### ⚠ ممنوع إعادة توليد صفحات الخدمات بلا فحص
`python3 scripts/build_services.py` يكتب فوق الصفحات المولَّدة. جُرِّب في 2026-09-24
فأضاف لـ `pos-system.html` قسم شعارات عملاء **كان محذوفًا عمدًا** (الصفحة اتعدّلت
يدويًا بعد التوليد في Commit `df46afc`). اتسترجعت بـ `git checkout`. القاعدة:
الناتج تعدّى المصدر، فأي تعديل على الصفحات المولَّدة **يتعمل في المصدر والناتج معًا
يدويًا**، ولا يُشغَّل السكربت إلا بعد `git diff` ومراجعة كل سطر.

## الدخول بالبصمة للمدير (Passkeys) — 2026-09-25
- زر «الدخول بالبصمة» في /admin/login (تبويب المدير فقط)، وزر «تفعيل الدخول بالبصمة» في الشريط الجانبي للوحة الإدارة (مرة لكل جهاز).
- المكتبة: @simplewebauthn/server و browser (v13). الملفات: src/lib/passkeys.js · src/app/auth/passkey/* · src/app/admin/PasskeySetup.js.
- الجدول public.admin_passkeys (مفاتيح عامة فقط، RLS بدون سياسات = service role فقط) — مُطبَّق على Supabase ‏kareempro-portal، والـ SQL في supabase/migrations/20260925_admin_passkeys.sql.
- بعد التحقق تُفتح جلسة Supabase عادية بنفس حيلة generateLink + verifyOtp المستخدمة في روابط الواتساب.
- rpID = الدومين بدون www، فالبصمة تشتغل على kareempro.com و www معًا.
- إصلاح: أيقونة العين كانت راكبة على نقط كلمة السر (الحقل dir=ltr) → padding-left في globals.css.
- محليًا: بعد git pull لازم npm install لأن فيه مكتبة جديدة.
